[phases.setup]
nixPkgs = ["python311", "python311Packages.pip", "tesseract", "poppler_utils", "libGL", "zlib"]

[phases.install]
cmds = ["python3.11 -m pip install -r requirements.txt"]

[start]
cmd = "gunicorn app:app --bind 0.0.0.0:$PORT --workers 2 --timeout 120"
